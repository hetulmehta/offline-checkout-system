# Offline Checkout System

#### Server setup:
```bash
cd server
npm install
node server
```

#### React-Native setup:
```bash
cd client\LevyneApp
yarn install
yarn react-native run-android
```
#### Web setup:
```bash
cd web
yarn install
yarn start
```
